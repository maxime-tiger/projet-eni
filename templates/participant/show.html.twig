{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/profil.css')}}">
{% endblock %}

{% block title %}Mon compte{% endblock %}

{% block main %}

    <div class="profil">

        <h1 class="title">Mon Profil</h1>
    
        <p>Nom : {{participant.nom}}</p>
        <p>prenom : {{participant.prenom}}</p>
        <p>Email : {{ participant.email }}</p>
        <p>Tel : {{ participant.telephone }}</p>
        <p>Campus : {{ participant.campus }}</p>

        <div class="btn">
            {# <a href="{{ path('app_participant_index') }}">Liste des participants</a> #}
            <a class="btn-modif btn-style" href="{{ path('app_participant_edit', {'id': participant.id}) }}">Modifier</a>
            <a class="btn-suppr btn-style" href="{{ path('app_participant_delete', {'id': participant.id}) }}">Supprimer</a>
        </div>
        

    </div>




{#} <h1>Mon profil</h1>
        
            <table class="table">
                <tbody>
                    {# <tr>
                        <th>Id</th>
                        <td>{{ participant.id }}</td>
                    </tr> 
                    <tr>
                        <th>Email</th>
                        <td>{{ participant.email }}</td>
                    </tr>
                    {# <tr>
                        <th>Roles</th>
                        <td>{{ participant.roles ? participant.roles|json_encode : '' }}</td>
                    </tr> #}
                    {# <tr>
                        <th>Password</th>
                        <td>{{ participant.password }}</td>
                    </tr> 
                    <tr>
                        <th>Nom</th>
                        <td>{{ participant.nom }}</td>
                    </tr>
                    <tr>
                        <th>Prenom</th>
                        <td>{{ participant.prenom }}</td>
                    </tr>
                    <tr>
                        <th>Telephone</th>
                        <td>{{ participant.telephone }}</td>
                    </tr>
                    <tr>
                        <th>Administrateur</th>
                        <td>{{ participant.administrateur ? 'Yes' : 'No' }}</td>
                    </tr>
                    <tr>
                        <th>Actif</th>
                        <td>{{ participant.actif ? 'Yes' : 'No' }}</td>
                    </tr>
                </tbody>
            </table>

            <a href="{{ path('app_participant_index') }}">Liste des participants</a>

            <a href="{{ path('app_participant_edit', {'id': participant.id}) }}">Modifier</a>
    {{ include('participant/_delete_form.html.twig') }}{#}


{% endblock %}
