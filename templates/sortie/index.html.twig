{% extends 'base.html.twig' %}

{% block title %}Sortie index{% endblock %}

{% block main %}
    <div class="sortieTitle">
        <h1>Les sorties </h1>
    </div>

        {% for sortie in sorties %}
            <div class="cardSorties" uk-grid>
                <div>
                    <div class="uk-card uk-card-default">
                        <div class="uk-card-media-top">
                            <img src="{{asset('images/mountains.svg')}}" width="" height="100%" alt="">
                        </div>
                        <div class="uk-card-body">
                            <h3 class="uk-card-title">Sortie à la montagne</h3>
                            <p>La montagne, ca vous gagne! </p>
                        </div>
                    </div>
                </div>
            </div>
        {% else %}
        <td>
            <a href="{{ path('app_sortie_show', {'id': sortie.id}) }}">show</a>
            <a href="{{ path('app_sortie_edit', {'id': sortie.id}) }}">edit</a>
        </td>
        <tr>
            <td colspan="9">no records found</td>
        </tr>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>DateHeureDebut</th>
                <th>Duree</th>
                <th>DateLimiteInscription</th>
                <th>NbInscriptionMax</th>
                <th>InfosSortie</th>
                <th>Etat</th>
                <th>actions</th>
            </tr>
        </thead>
        {% endfor %}
        

    <a href="{{ path('app_sortie_new') }}">Create new</a>
{% endblock %}
